{
  "name": "@toptanportal/db",
  "version": "1.0.0",
  "private": true,
  "description": "Prisma semasi, istemci ve veritabani sertlestirme betikleri",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "prisma"],
  "scripts": {
    "generate": "prisma generate",
    "build": "prisma generate && tsc -p tsconfig.json",
    "lint": "eslint \"src/**/*.ts\" --max-warnings 0",
    "migrate:dev": "dotenv -e ../../.env -- prisma migrate dev && pnpm harden",
    "migrate:deploy": "dotenv -e ../../.env -- prisma migrate deploy && pnpm harden",
    "harden": "dotenv -e ../../.env -- ts-node --transpile-only src/scripts/harden.ts",
    "verify-audit": "dotenv -e ../../.env -- ts-node --transpile-only src/scripts/verify-audit-chain.ts",
    "seed": "dotenv -e ../../.env -- ts-node --transpile-only prisma/seed.ts",
    "studio": "dotenv -e ../../.env -- prisma studio"
  },
  "prisma": {
    "seed": "ts-node --transpile-only prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "5.20.0",
    "@toptanportal/contracts": "workspace:*",
    "argon2": "0.41.1"
  },
  "devDependencies": {
    "dotenv-cli": "7.4.2",
    "prisma": "5.20.0",
    "ts-node": "10.9.2",
    "typescript": "5.6.3",
    "@types/node": "22.7.5"
  }
}
